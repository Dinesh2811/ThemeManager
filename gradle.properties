# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

#android.enableR8.fullMode=true


#android.defaults.buildfeatures.buildconfig=true
#android.nonFinalResIds=false
#org.gradle.unsafe.configuration-cache=true
#
## Enable Gradle Build Cache
#org.gradle.caching=true
#
## Other properties
#org.gradle.parallel=true
#org.gradle.configureondemand=true
#org.gradle.daemon=true
#org.gradle.configuration-cache=true
#createReports=true
#org.gradle.jvmargs=-Xmx6g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:MaxMetaspaceSize=1g
#kotlin.incremental.useClasspathSnapshot=true
#org.gradle.workers.max=12
#
#
#
### gradle.properties for debugging
##kotlin.build.report.enable=true
##kotlin.build.report.verbose=true
##
### gradle property for metrics
##kotlin.build.report.metrics=true
#
#
#
#
##   ./gradlew assemble --build-cache        #   Build and populate cache
##   ./gradlew clean                         #   Remove/change build outputs
#
#
#
#
#
#android.enableJetifier=true
#org.gradle.caching.debug=true
#org.gradle.workers.caching=true
## Optimize dependency resolution
#org.gradle.dependency.cacheable=true
## Enable build scans for insights
#org.gradle.buildscan.commercial=true
## Use parallel threads for dexing
#org.gradle.dexOptions.parallelThreads=4
## Configure Kotlin incremental compilation
#kotlin.incremental.usePreciseJavaTracking=true
#
#
#
## Optimize task execution
#org.gradle.parallel.intra=true
#org.gradle.parallel.tasks.inTheSameProject=true
## Control the number of concurrent builds
#org.gradle.project.parallel=true
## Use file system watching for faster incremental builds
#org.gradle.daemon.performance.watch-fs=true
#
#
#
#
## Use the Android Gradle Plugin's worker API
#android.useAndroidGradlePluginWorkers=true
## Enable Gradle's new configuration cache
#org.gradle.caching.configureOnDemand=true
## Optimize Lint checks
#android.lint.cache=true
#android.lint.performPredex=true
## Use Composite Build to speed up development builds
#org.gradle.composite=true
## Improve resource compilation speed
#android.enableResourceOptimizations=true
